version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15
    working_directory: ~/services/webapp
    steps:
      - checkout
      - run: npm install
      - run: npm run lint
      - run: npm run test
      - run: npm run build
